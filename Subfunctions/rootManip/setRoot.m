%return variables to root (debug function)

%--------------------------------------------------------------------------
% FlyFly v2
%
% Jonas Henriksson, 2010                                   info@flyfly.se
%--------------------------------------------------------------------------

setappdata(0, 'chstimuli',  chstimuli);  %Load list of chosen stims
setappdata(0, 'navData',    navData);    %Load navigation data
setappdata(0, 'screenData', screenData);    %Load navigation data